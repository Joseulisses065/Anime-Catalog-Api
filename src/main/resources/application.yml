cors:
  originPatterns: http://localhost:3000,http://localhost:8080
spring:
  application:
    name: animeapi
  datasource:
    url: "jdbc:postgresql://localhost:5432/animedb"
    username: docker
    password: docker
  jpa:
    hibernate:
      ddl-auto: update